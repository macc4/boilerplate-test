export const ALLOWED_CORS_ORIGINS = {
  dev: ['http://localhost:3000'],
  qa: [],
  prod: [],
};

export const SUPPORTED_IMAGE_FILE_TYPES_REGEX = /(jpeg|png|bmp|jpg)$/;

export const MAX_FILE_SIZE = 2 * 1024 * 1024; // 2 MB
